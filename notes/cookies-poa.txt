- persist state on page refresh
  - this will make sure the check for user data in $store exists and no need to check cookies
- if no user data found in $store
  - if no cookies set
    - redirect to login page and login as normal
    - if agreed to cookies  
      - save cookies after redirect with encoded Spotify user id
  - if cookies set
    - get cookie for Spotify user id
    - send to backend (decode in frontend or backend?)
    - get user data from mongo based on Spotify user id
    - refresh access token
    - if no data found (error)
      - redirect to Spotify login page with error and let user know there was an issue getting their data and please login to Spotify again

    